dependencies:
  pre:
    - npm install mocha -g

checkout:
  post:
    - git clone https://github.com/D-Andreev/mysql-adapter.git
    - git checkout master


test:
  override:
    - cd ./mysql-adapter && npm run-script test-unit
