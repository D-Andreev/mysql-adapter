dependencies:
  override:
    - coffee -c .
    - rm -r ./node_modules
    - ./node_modules/coffeelint/bin/coffeelint -f coffeelint.json .
    - npm install

test:
  override:
    - npm run-script test-unit
